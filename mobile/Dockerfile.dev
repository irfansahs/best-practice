FROM node:22-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 8081

# Bind-mounted source + named node_modules volume: ensure deps on each start.
CMD ["sh", "-c", "npm ci && npx expo start --web --port 8081 --host lan"]
